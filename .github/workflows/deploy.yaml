name: "my-job"

on: 
  push: 
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hola Mundo" >> hola.txt
  second-job:
    needs: my-job
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: cat hola.txt
